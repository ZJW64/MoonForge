// MoonForge — a source-to-source derive code generator for MoonBit.
// Configuration reference: https://docs.moonbitlang.com/en/latest/toolchain/moon/module.html

name = "moonforge"

version = "0.1.0"

readme = "README.md"

repository = ""

license = "Apache-2.0"

keywords = [ "codegen", "derive", "metaprogramming", "tooling" ]

preferred_target = "js"

description = "Source-to-source derive code generator for MoonBit"

import {
  "moonbitlang/x@0.5.5",
}
