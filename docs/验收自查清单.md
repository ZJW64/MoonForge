# 验收自查清单 · MoonForge

> 用法：评委（或作者自己）照着这张表逐项跑一遍即可。每一行都写明**复现命令**与**实测结果**，
> 不写"应该"、只写"跑出来是什么"。

## 0. 环境基线

| 项 | 值 |
|---|---|
| OS | Windows 11（本项目主要在 Windows 开发，见下"跨平台"一行） |
| `moon` | `0.1.20260915 (2e1a46d 2026-09-15)` |
| `moonc` | `v0.10.13+cbb11c36f (2026-09-15)` |
| `node` | `v22.22.2` |
| 仓库 | <https://github.com/ZJW64/MoonForge>（公开） |

## 1. 交付件是否齐全

| 验收项 | 交付件 | 位置 |
|---|---|---|
| 公开仓库 | GitHub 仓库（公开，Apache-2.0） | <https://github.com/ZJW64/MoonForge> |
| 项目说明（README） | 含背景、用法、规则、架构、测试策略、已知限制、验收对照 | [`README.md`](../README.md) |
| 一页项目说明 | 一页纸说清问题 / 方案 / 交付范围 / 可验证证据 / 创新点 / Roadmap | [`docs/一页项目说明.md`](一页项目说明.md) |
| 可复现演示说明 | 5 分钟走完全流程，含每一步的真实输出 | [`docs/演示说明.md`](演示说明.md) |
| 项目方案（完善版） | 取舍论证、阻塞点、Roadmap | [`docs/项目方案（完善版）.md`](项目方案（完善版）.md) |
| 扩展规则指南 | 15 分钟给工具加一条规则 | [`docs/扩展规则指南.md`](扩展规则指南.md) |
| 贡献指南 | 开发流程 / 提交信息规范 / 破坏性变更约定 | [`CONTRIBUTING.md`](../CONTRIBUTING.md) |
| 变更日志 | Keep a Changelog 格式，逐条记录修复与验证方式 | [`CHANGELOG.md`](../CHANGELOG.md) |
| 项目申报书 | 基本信息 / 简介 / 场景 / 核心功能 / 验收产物 / 边界 | `MoonForge-项目申报书.md` |
| 可运行示例 | 示例输入 + **已提交的生成物** + 直接调用生成代码的测试 | [`examples/`](../examples) |
| CI | GitHub Actions，ubuntu + windows 双平台 | [`.github/workflows/ci.yml`](../.github/workflows/ci.yml) |
| 许可证 | Apache-2.0 | [`LICENSE`](../LICENSE) |

## 2. 能编译 / 能测试 / 能运行

| 验收项 | 命令 | 实测结果 |
|---|---|---|
| 编译通过（含自动生成的代码） | `moon check` | ✅ `Finished. moon: no work to do` |
| 单元 + 金样 + 幂等 + 集成测试 | `moon test --target js` | ✅ `Total tests: 96, passed: 96, failed: 0.` |
| 换后端再跑一遍 | `moon test --target wasm-gc` | ✅ `Total tests: 96, passed: 96, failed: 0.` |
| 一条命令复现全部检查 | `bash scripts/e2e.sh` | ✅ 6 个阶段全过 |

## 3. `scripts/e2e.sh` 的 6 个阶段分别证明了什么

| 阶段 | 证明的主张 | 失败会意味着什么 |
|---|---|---|
| 1 漂移检查 | 仓库里已提交的生成物与当前源文件**一致**；位置参数结构体没有产出任何方法 | 生成物过时，或位置参数结构体又"借用"了下一个声明的字段体 |
| 2 测试 | 96 个用例全过 | 词法 / 语法 / 规则 / 幂等 / 集成某一层退化 |
| 3 真编译 | 生成出来的代码**能被编译器接受** | 生成器输出了非法 MoonBit —— 最严重的一类缺陷 |
| 4 格式化稳定性 | 仓库已格式化；且生成物与 `moon fmt` 的字节**一致** | 用户跑一次 `moon fmt` 就会让 `check` 永久报漂移，而 `gen` 修不好 |
| 5 CRLF 容忍 | 生成物被换成 CRLF 后 `check` 仍通过 | Windows 上（`core.autocrlf=true`）会误报漂移 |
| 6 闭环与退出码契约 | 漂移 → `1`；`gen` 后 → `0`；路径错 / 非 `.mbt` / 传目录 → `2`；去掉 `@derive` 后 `gen` 清理残留生成物 | CI 会"假绿"，或用户陷入"照提示跑 `gen` 也修不好"的死循环 |

## 4. "工作有效"这件事怎么证明

不是靠自我声明，而是三条可执行的证据链：

| 主张 | 怎么验（命令） | 为什么这是证据 |
|---|---|---|
| 生成代码真的能用 | `moon test --target js`，其中 `examples/models_wbtest.mbt` 直接调用 `User::validate` / `User::diff` / `User::field_names` / `User::to_pairs` | 这些方法在仓库里**没有任何手写实现**——唯一来源是生成器 |
| 生成代码真的合法 | `rm examples/models_derive_gen.mbt && moon check` → 失败；`moon run cmd/main -- gen examples && moon check` → 恢复 | 正确性由**编译器**裁决，不由工具的自我声明裁决 |
| 漂移真的能被拦住 | `moon run cmd/main -- check <改了源码的副本>` → 退出码 `1` | CI 的核心价值；这条负例在 e2e 第 6 阶段被固化 |

## 5. "可扩展"这件事怎么证明

| 主张 | 怎么验 | 证据 |
|---|---|---|
| 规则是公开的普通值，不是插件配置 | 读 [`rule.mbt`](../rule.mbt) 的 `Rule` 定义 | `Rule { name, description, gen : (TypeDef) -> String? }`，无任何注册宏或代码生成魔法 |
| 加规则不用碰核心代码 | 换 `registry.mbt` 的那一行 `[field_names(), validate(), diff(), to_pairs()]` | `to_pairs` 就是按这个流程加进来的第 4 条规则：**1 个新文件 + 注册表 1 项 + 一组测试**，`lexer.mbt` / `parser.mbt` / `codegen.mbt` 一行未改 |
| 有可照抄的完整流程 | 读 [`docs/扩展规则指南.md`](扩展规则指南.md) | 含契约、三段式骨架、白名单纪律、反面例子、收尾自查清单 |

## 6. "AI 可解释 / 工程判断"这件事怎么证明

| 主张 | 证据位置 |
|---|---|
| 明确划清"不做什么" | `README.md` 第 2 节：不是编译器插件、不发明语法、不做运行时反射、不是 comptime |
| 已知限制如实列出（9 条） | `README.md` 第 9 节 |
| 关键取舍留了"为什么"和"被放弃的方案" | `README.md` 第 5.2 节相关的 `docs/项目方案（完善版）.md`；代码注释里如 `type_model.mbt` 对 `BodyKind` 的解释 |
| 阻塞点明确标出（而不是假装能做完） | `docs/一页项目说明.md` 的 Roadmap：AST 级生成需编译器开放 AST（当前不存在）、comptime 需编译器支持（当前不存在） |

## 7. 提交记录是否满足验收

```bash
$ git rev-list --count HEAD
<n>

$ git log --oneline
<...>
```

提交按 `feat` / `fix` / `ci` / `docs` / `chore` / `style` 分类，每条的正文都写了
**"原来是什么行为 / 为什么改 / 怎么验证"**。规范见 [`CONTRIBUTING.md`](../CONTRIBUTING.md) 第 3 节。

## 8. 复现步骤（给评委的最短路径）

```bash
git clone https://github.com/ZJW64/MoonForge && cd MoonForge
moon check                                # 1. 含生成代码一起编译
moon run cmd/main -- explain examples/models.mbt   # 2. 看工具如何解释自己
moon test --target js                     # 3. 96 个用例
bash scripts/e2e.sh                       # 4. 6 个阶段端到端
```

第 2 步的输出值得一看：它会把 `Point` 显示为 `struct(位置参数)`，并对三条规则各给一句
`[--] ... 不适用：Point 是位置参数（元组）结构体，没有具名字段`。
**"不生成"和"生成"一样要被解释**——这是本项目对"可解释"的具体理解。

## 9. 已知的、非缺陷的环境限制

| 现象 | 说明 |
|---|---|
| `moon test --target native` 报 `no system C compiler found` | 本机没有 C 编译器。`js` 与 `wasm-gc` 两个后端均 96/96 通过；`native` 后端不是本项目的验收口径 |
| `moon fmt --check` 在"有差异"时退出码为 `127`（不是 `1`） | MoonBit 工具链的实际行为，CI 只判定非零。已在 `.github/workflows/ci.yml` 注释中写明，避免后来人误判为脚本 bug |
| `wasm-gc` 后端的出错路径会附带 `RuntimeError: unreachable` 回溯 | `wasm-gc` 没有标准退出原语。因此 CI 的漂移检查固定用默认的 `js` 后端 |
