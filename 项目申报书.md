# MoonForge 月铸：MoonBit 自定义派生代码生成器

## 基本信息

- 项目名称：MoonForge 月铸：MoonBit 自定义派生代码生成器
- 参赛者：张佳伟
- 联系方式：15383279220
- GitHub 仓库链接：https://github.com/ZJW64/MoonForge
- 项目方向：MoonBit 语言与开发工具／元编程基础设施
- 是否为原创项目：原创，非移植，无参考实现
- 通用性说明：规则是普通值，请求写在 doc comment 里，换类型或换后端都不动核心逻辑。

## 项目简介

MoonBit 的 `derive()` 只能派生内置 trait（E4077）；MoonForge 在用户态补齐这个注册点。

## 预期使用场景

- 接口校验：`@derive(validate)` 加 `@nonempty`/`@min`/`@max` 即得 `User::validate()`。
- 审计与展示：`diff` 返回变化字段名，`field_names`/`to_pairs` 给出字段清单与键值对。

## 拟实现的核心功能

- 词法与子集语法分析：只认花括号深度 0 的 `struct`/`enum`；IR 用 `BodyKind` 判定有无具名字段。
- 类型模型 IR 与规则接口：规则名、说明、生成函数三件套，`Rule` 是公开的普通值。
- 四条内置规则：`field_names`、`validate`（注解驱动）、`diff`、`to_pairs`。
- CLI：`list-rules` / `explain` / `gen` / `check`；生成物字节稳定，漂移即非零退出（0/1/2）。

本轮另修掉位置参数结构体吞并后续声明、`moon fmt` 后永久报漂移、CRLF 误报等 7 处缺陷。

## 预期验收产物

`moon test`（js / wasm-gc）各 96 条全绿；`scripts/e2e.sh` 6 阶段全过：漂移 / 真编译 / 格式化稳定性 / CRLF 容忍 / 退出码契约；删生成文件则 `moon check` 失败；CI 双平台全绿。

## 原创或移植说明

原创，非移植，无第三方代码，Apache-2.0。灵感来自 Go `stringer`、Rust `serde`、Zig `comptime`。

## 项目价值与生态定位

派生注册表在编译器内部硬编码，AST、proc-macro、comptime 均未公开，「为自定义类型生成代码」只能手写：重复、易漏、随字段漂移。MoonForge 把注册点补在用户态，让**派生规则可被外部扩展**。

## 交付范围与工程边界

交付一条可编译、可测试、可被 CI 校验的生成链路，而非编译期元编程框架；明确不做编译器插件、运行时反射、comptime。纪律是只对类型名白名单内的类型生成代码，不确定就跳过并留说明行。

## 实现路径与技术理解

路径只有两条：等编译器开放插件接口，或在用户态做源码生成；前者短期落不了地，后者今天能跑。三点理解：生成物须与源文件同目录——包边界即目录，私有字段仅同包可见；请求写成 doc comment，不新增语法；生成物进仓库，由 `moon check` 裁决正确性。
