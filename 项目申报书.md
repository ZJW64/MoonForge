# MoonForge 月铸：MoonBit 自定义派生代码生成器

> 文档状态：**本期已交付**。文中标注「已完成」的条目均可在仓库中复现，验收方法与实测结果见
> [`docs/验收自查清单.md`](docs/验收自查清单.md)。

## 基本信息

- 项目名称：MoonForge 月铸：MoonBit 自定义派生代码生成器
- 参赛者：张佳伟
- 联系方式：15383279220
- GitHub 仓库链接：https://github.com/ZJW64/MoonForge
- 项目方向：MoonBit 语言与开发工具／元编程基础设施
- 是否为原创项目：原创，非移植，无参考实现
- 许可证：Apache-2.0
- 通用性说明：规则是普通值，请求写在 doc comment 里，换类型或换后端都不动核心逻辑。内置 4 条规则之外，第三方可把本包作为库依赖注入自己的规则集合。

## 项目简介

MoonBit 的 `derive()` 只能派生编译器内置的 trait（官方错误码 E4077 明写「你无法让 MoonBit 自动派生你自定义的特征」）。MoonForge 在用户态补齐这个缺失的派生注册点：读取 `.mbt` 里的类型定义与 `/// @derive(...)` 标记，按可插拔规则生成 MoonBit 源码，写回源文件同目录，与手写代码一起参与编译。**100% MoonBit 实现**，不修改编译器、不依赖编译器内部 API。

## 预期使用场景

- **接口校验**：`User` 标 `@derive(validate)`，配合字段注解 `@nonempty` / `@min(0)` / `@max(150)` 即得 `User::validate() -> Result[Unit, String]`，字段增删不必再手改校验代码。
- **变更审计**：`@derive(diff)` 生成 `T::diff(a, b) -> Array[String]`，返回发生变化的字段名，用于配置比对与增量同步。
- **表单渲染**：`@derive(field_names)` 生成 `T::field_names()`，前端据此渲染，无须另存一份字段清单。
- **键值摊平**：`@derive(to_pairs)` 生成 `T::to_pairs() -> Array[(String, String)]`，把结构体摊平成「字段名 → 字段值」，喂给表单、日志、审计或只认字符串的接口。

## 拟实现的核心功能（均已实现）

- **词法与子集语法分析**：只认花括号深度 0 的 `struct` / `enum`，函数体整体跳过；IR 用 `BodyKind` 区分「有具名字段 / 位置参数 / 无字段体」，使「按字段名展开」的规则能正确判断适用性。
- **类型模型 IR 与规则接口**：规则名、说明、生成函数三件套，`Rule` 是公开的普通值；`gen` 返回 `None` 表示「不适用」而非错误。
- **四条内置规则**：`field_names`、`validate`（注解驱动）、`diff`、`to_pairs`。
- **CLI**：`list-rules` / `explain` / `gen` / `check`；生成物字节稳定，漂移即非零退出，退出码契约为 `0` 成功 / `1` 检出漂移 / `2` 参数或路径有误。
- **工程闭环**：生成物提交进仓库 → `moon check` 真实编译它 → `check` 子命令比对源文件与生成物的字节，拦住「改了源码却忘记重新生成」。

本次冲刺另外修掉了若干会导致「看似通过、实则生成错代码」的缺陷（位置参数结构体吞并后续声明、`moon fmt` 后永久报漂移、Windows CRLF 误报、尾随逗号、单行结构体注解泄漏、错误路径静默返回 0、`gen` 无法清理残留生成物等），逐条记在 [`CHANGELOG.md`](CHANGELOG.md)。

## 预期验收产物（实测结果）

| 验收项 | 命令 | 实测结果 |
|---|---|---|
| 编译通过（含自动生成的代码） | `moon check` | ✅ 通过 |
| 测试 | `moon test --target js` | ✅ 96 个用例全过 |
| 换后端再跑 | `moon test --target wasm-gc` | ✅ 96 个用例全过 |
| 端到端 | `bash scripts/e2e.sh` | ✅ 6 个阶段全过（漂移 / 测试 / 真编译 / 格式化稳定性 / CRLF 容忍 / 退出码契约） |
| 反证：删掉生成文件 | `rm examples/models_derive_gen.mbt && moon check` | ✅ 编译失败（证明编译检查确实覆盖生成代码） |
| CI | GitHub Actions | ✅ ubuntu 与 windows 双平台，含格式化检查与双后端测试 |

提交记录按 `feat` / `fix` / `ci` / `docs` / `chore` / `style` 分类，每条提交信息都写了「原来是什么行为 / 为什么改 / 怎么验证」。规范见 [`CONTRIBUTING.md`](CONTRIBUTING.md)，Issue / PR 模板见 [`.github/`](.github/)。

## 原创或移植说明

原创，非移植，**未复制任何第三方代码**，许可证 Apache-2.0。设计灵感来自 Go `stringer` / `go generate`（生成物进仓库 + CI 查漂移）、Rust `serde` / `quote`（声明式派生）、Zig `comptime`（编译期生成的动机），仅在 [`README.md`](README.md) 第 11 节致谢，无代码层面的借鉴。

## 项目价值与生态定位

派生注册表在编译器内部硬编码（`src/ast_derive.ml` 是一张 `Trait → Deriver` 哈希表），AST、proc-macro、comptime 均未公开，因此「为自定义类型批量生成语义化代码」目前只能手写：重复、易漏、随字段变更而漂移。MoonForge 不要求编译器开口子，而是把注册点补在用户态，并把它做成可测试、可持续维护的工程资产——生成物进版本库、由编译器裁决正确性、由 CI 拦住漂移。这套做法的价值不在于某一条具体规则，而在于**「派生规则」这件事本身可以被外部扩展**，而这正是当前 MoonBit 生态缺失的一环。

## 交付范围与工程边界

交付一条可编译、可测试、可被 CI 校验的完整生成链路，而不是编译期元编程框架。**明确不做**：编译器插件、运行时反射、comptime，也不声称扩展了 `derive()`（`derive()` 的行为完全不受影响）。

诚实列出 9 条已知限制（见 `README.md` 第 9 节），其中包括「只解析顶层类型定义」「字段类型不做语义分析」「目录只扫描一层」等。对能力边界的态度是：**只对类型名白名单内的类型生成代码，不确定就跳过并在生成文件里留下 `// moonforge:` 说明行**——「为什么不生成」和「生成了什么」一样是产品的一部分。因为生成物要被 `moon check` 真实编译，「生成编译不过的代码」比「少生成代码」严重得多。

## 实现路径与技术理解

路径只有两条：等编译器开放插件接口，或在用户态做源码生成；前者短期落不了地，后者今天能跑。本项目选后者，并把取舍讲清楚。

四点关键理解：

1. **生成物必须与源文件同目录**——MoonBit 的包边界就是目录，私有字段仅同包可见。生成到同目录才能直接访问字段，而不强迫用户把字段改成 `pub`；
2. **请求写成 doc comment**——不新增 attribute、不发明语法，IDE 与 `moon fmt` 都不报错，是零侵入的；
3. **生成物进仓库、由编译器裁决正确性**——把「工具输出的代码是对的」从「我说它对」变成「编译器说它对」。同时 `check` 子命令比对字节以检出漂移，这要求生成器输出**字节稳定**且与 `moonfmt` 的形状**一致**（否则用户格式化一次就会让检查永久误报）；
4. **规则是一等公民**——`Rule` 是公开的普通值，加一条规则 = 加一个文件 + 注册表加一项 + 补一组测试，不触碰词法 / 语法 / 代码生成任何核心代码。`to_pairs` 就是按这个流程加进来的第 4 条规则，可作实证。

对「工程边界」的理解体现在 Roadmap 上：v1.0 的 AST 级生成与 v2.0 的 comptime 都明确标出**阻塞点**（都需要编译器先开放能力，当前不存在），而不是假装能做完。
